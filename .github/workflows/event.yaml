name: Push Events
on:
  issues:
    types:
      - opened
jobs:
  show-info:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "This is a push event on the main or test branch"
